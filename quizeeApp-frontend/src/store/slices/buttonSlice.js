// // buttonSlice.js
// import { ADD_BUTTON, REMOVE_BUTTON } from "../actions/types";

// const intialState = {
//   buttonCount: 1,
// };

// const buttonReducer = (state = intialState, action) => {
//   switch (action.type) {
//     case ADD_BUTTON:
//       return {
//         value: (state.buttonCount += 1),
//       };
//     case REMOVE_BUTTON:
//       return {
//         value: (state.buttonCount -= 1),
//       };
//     default:
//       return state;
//   }
// };
// export default buttonReducer;

// // export const { addButton, removeButton } = buttonSlice.actions;
// // export default buttonSlice.reducer;
